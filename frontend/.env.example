# Frontend configuration. Copy to .env.local and fill in after deployment.
# The contract address is public (safe to commit if you choose); it is NOT a secret.
VITE_CONTRACT_ADDRESS=
VITE_NETWORK=testnetBradbury
